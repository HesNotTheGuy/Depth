{
  "name": "depth-figma-plugin",
  "version": "0.1.0",
  "description": "Figma plugin for Depth — 3D mockups for 2D designers.",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -p . --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "typescript": "^5.4.0"
  }
}
