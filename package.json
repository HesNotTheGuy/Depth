{
  "name": "depth-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Depth — 3D compositing for 2D images. Web app + Electron desktop + native C++ SDK.",
  "workspaces": [
    "web",
    "electron",
    "figma-plugin"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:web dev:electron",
    "dev:web": "npm --workspace web run dev",
    "dev:electron": "npm --workspace electron run dev",
    "build": "npm-run-all build:web build:sidecar build:electron",
    "build:web": "npm --workspace web run build",
    "build:sidecar": "cmake -S sdk -B sdk/build -DDEPTH_BUILD_EXAMPLES=OFF -DDEPTH_BUILD_TESTS=OFF && cmake --build sdk/build --target depth_sidecar --config Release",
    "build:electron": "npm --workspace electron run build",
    "package": "npm --workspace electron run package"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
